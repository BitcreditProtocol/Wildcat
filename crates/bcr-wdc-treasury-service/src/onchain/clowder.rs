// ----- standard library imports
use std::sync::Arc;
// ----- extra library imports
// ----- local imports
use crate::{
    error::{Error, Result},
    onchain::ClowderClient,
};

// ----- end imports
